$id: "character.schema.yaml"
$schema: "http://json-schema.org/draft-07/schema#"

title: CharacterData
type: object
properties:
  slots:
    type: object
    additionalProperties:
      type: object
      properties:
        holds:
          type: array
          items:
            $ref: "item_types.schema.yaml"
      required:
        - holds

required:
  - slots
additionalProperties: false
